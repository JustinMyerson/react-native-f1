import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  resultList: {
    marginLeft: 10,
    marginTop: 10,
  },

  result: {
    marginTop: 10,
    marginBottom: 5,
  },

  heading: {
    display: "flex",
    alignItems: "center",
  },
});
