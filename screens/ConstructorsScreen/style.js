import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  constructorsList: {
    marginLeft: 10,
    marginTop: 10,
  },

  constructor: {
    marginBottom: 5,
  },

  buttons: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
  },
});
